# Implementation Phases

**Project:** Md Adnan Umar — GitHub Profile  
**Last Updated:** 2026-07-22

---

## Phase 0 — Planning ✅

- Defined project goals, design language, and folder structure
- Chose SMIL-only animation strategy
- Documented colour palette and layout system
- Created `docs/` structure with 6 planning documents
- Wrote `.gitignore` and `requirements.txt`
- Extracted existing base64 avatar into `scripts/pic-b64.txt`

**Deliverables:** `docs/`, `.gitignore`, `requirements.txt`, `scripts/pic-b64.txt`

---

## Phase 1 — Hero SVG Assets ✅

- Hand-crafted `dark.svg` (1180×610, ~141.7 KB)
  - Circular photo with animated gradient ring
  - Terminal-style right panel with code block, metric cards, what-I-ship cards
  - SMIL animations: gradient shift, scanline, noise, particles, border shimmer, typing cursor
- Hand-crafted `light.svg` (1180×610, ~141.7 KB)
  - Same structure as dark, light colour tokens
- `info-card.svg` (760×310, ~133.2 KB)
  - Neofetch-style terminal card with avatar, info lines, skill pills, highlights
  - Generated by `make_info_card.py` with embedded base64 avatar
- Verified: photo embedded ✓, animations present ✓, SVG properly closed ✓

**Deliverables:** `dark.svg`, `light.svg`, `info-card.svg`

---

## Phase 2 — Photo & ASCII Scripts ✅

- `scripts/prep_photo.py`: removes background with `rembg`, boosts contrast with CLAHE
- `scripts/make_ascii_svg.py`: converts prepped photo to animated monochrome ASCII SVG
  - Density ramp mapped to brightness
  - Clip-path reveal, gradient text, floating animation, cursor blink

**Deliverables:** `scripts/prep_photo.py`, `scripts/make_ascii_svg.py`

---

## Phase 3 — Contribution Graph & Automation ✅

- `scripts/fetch_contributions.py`:
  - Multi-selector strategy (`td[data-date]`, `rect[data-date]`)
  - Count extraction from `data-count`, `<title>`, and text
  - Proper User-Agent headers
- `scripts/render_heatmap_svg.py`:
  - 801×191 canvas with 53×7 grid
  - Month/day labels, 6-level green ramp
  - Diagonal stagger animation, legend, stats footer
- `contrib-heatmap.svg` generated from live GitHub data
- `.github/workflows/update-profile.yml`: daily cron at 06:17 UTC
- `.github/workflows/snake.yml`: every 12 hours via Platane/snk

**Deliverables:** `contrib-heatmap.svg`, `scripts/fetch_contributions.py`, `scripts/render_heatmap_svg.py`, `.github/workflows/update-profile.yml`, `.github/workflows/snake.yml`

---

## Phase 4 — README ✅

- `README.md` rewritten with 14 terminal-style sections:
  - Hero banners with `<picture>` dark/light switching
  - `$ whoami`, `$ cat about.java`, `$ neofetch`
  - Education, experience, focus, tech-stack, projects
  - Open source, GitHub stats, contribution graph, achievements
  - Timeline, connect, dev quote, footer
- No JavaScript. No external CSS. All animation is SMIL-only.

**Deliverables:** `README.md`

---

## Phase 5 — Validation & Polish ✅

- Fixed `make_info_card.py` Windows encoding issue
- Fixed `make_info_card.py` to fallback to extracting base64 from existing `info-card.svg`
- Synchronized `docs/architecture.md` with actual repository contents
- Updated `docs/memory.md` asset sizes and removed stale references
- Validated: file references, Markdown formatting, SVG compatibility

**Deliverables:** Production-ready repository
